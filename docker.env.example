# SQL Server is running on the local machine, outside Docker Desktop.
# Do not use localhost here; inside the container, localhost means the container itself.
HOST=host.docker.internal
PORT=1433
USER=replace_with_sql_user
PASSWORD=replace_with_sql_password
DATABASE=CTS_DEV
